# @package _global_
defaults:
  - _self_

# ============================================================================
# Paths Configuration — UPDATE ALL FIELDS BELOW BEFORE TRAINING
# ============================================================================
paths:
  # All paths below are relative to the repository root (the directory you cd into
  # before running python sam3/train/train.py).
  #
  # Populate sam3/train/data/irrigation_canal/ with your data BEFORE training:
  #
  #   sam3/train/data/irrigation_canal/
  #   ├── train/
  #   │   ├── images/   ← your training satellite PNG images
  #   │   └── masks/    ← corresponding binary PNG masks
  #   ├── val/
  #   │   ├── images/
  #   │   └── masks/
  #   └── test/          (optional)
  #       ├── images/
  #       └── masks/
  #
  # Then run scripts/convert_masks_to_coco.py (see README.md Step 2) to generate
  # the _annotations.coco.json files inside each split folder.

  # Directory that contains your train/, val/ subfolders (each with an images/ subdir).
  images_root: sam3/train/data/irrigation_canal

  # Directory that contains the _annotations.coco.json files generated by
  # scripts/convert_masks_to_coco.py (one per split subfolder).
  # If you ran convert_masks_to_coco.py without --output_dir, this equals images_root.
  coco_root: sam3/train/data/irrigation_canal

  # Directory where checkpoints, logs, and TensorBoard files will be saved.
  # Created automatically if it does not exist.
  experiment_log_dir: experiments/irrigation_canal

  # BPE vocabulary file shipped with this repository — no changes needed.
  bpe_path: sam3/assets/bpe_simple_vocab_16e6.txt.gz

  # Leave as null to download SAM3 weights automatically from HuggingFace on the first run.
  # After the first run the weights are cached at:
  #   ~/.cache/huggingface/hub/models--facebook--sam3/snapshots/<hash>/sam3.pt
  # Set to that absolute path to skip re-downloading on subsequent runs.
  checkpoint_path: null

# ============================================================================
# Dataset configuration
# ============================================================================
irrigation_train:

  # Training transforms pipeline
  train_transforms:
    - _target_: sam3.train.transforms.basic_for_api.ComposeAPI
      transforms:
        - _target_: sam3.train.transforms.filter_query_transforms.FlexibleFilterFindGetQueries
          query_filter:
            _target_: sam3.train.transforms.filter_query_transforms.FilterCrowds
        - _target_: sam3.train.transforms.point_sampling.RandomizeInputBbox
          box_noise_std: 0.1
          box_noise_max: 20
        - _target_: sam3.train.transforms.segmentation.DecodeRle
        # Random horizontal flip — safe for aerial imagery (no dominant orientation)
        - _target_: sam3.train.transforms.basic_for_api.RandomHorizontalFlip
          consistent_transform: ${scratch.consistent_transform}
          p: 0.5
        # Mild colour jitter — improves robustness to seasonal / sensor variation
        - _target_: sam3.train.transforms.basic_for_api.ColorJitter
          consistent_transform: ${scratch.consistent_transform}
          brightness: 0.2
          contrast: 0.2
          saturation: 0.1
          hue: null
        - _target_: sam3.train.transforms.basic_for_api.RandomResizeAPI
          sizes:
            _target_: sam3.train.transforms.basic.get_random_resize_scales
            size: ${scratch.resolution}
            min_size: 480
            rounded: false
          max_size:
            _target_: sam3.train.transforms.basic.get_random_resize_max_size
            size: ${scratch.resolution}
          square: true
          consistent_transform: ${scratch.consistent_transform}
        - _target_: sam3.train.transforms.basic_for_api.PadToSizeAPI
          size: ${scratch.resolution}
          consistent_transform: ${scratch.consistent_transform}
        - _target_: sam3.train.transforms.basic_for_api.ToTensorAPI
        - _target_: sam3.train.transforms.filter_query_transforms.FlexibleFilterFindGetQueries
          query_filter:
            _target_: sam3.train.transforms.filter_query_transforms.FilterEmptyTargets
        - _target_: sam3.train.transforms.basic_for_api.NormalizeAPI
          mean: ${scratch.train_norm_mean}
          std: ${scratch.train_norm_std}
        - _target_: sam3.train.transforms.filter_query_transforms.FlexibleFilterFindGetQueries
          query_filter:
            _target_: sam3.train.transforms.filter_query_transforms.FilterEmptyTargets
    - _target_: sam3.train.transforms.filter_query_transforms.FlexibleFilterFindGetQueries
      query_filter:
        _target_: sam3.train.transforms.filter_query_transforms.FilterFindQueriesWithTooManyOut
        max_num_objects: ${scratch.max_ann_per_img}

  # Validation transforms pipeline
  val_transforms:
  - _target_: sam3.train.transforms.basic_for_api.ComposeAPI
    transforms:
      - _target_: sam3.train.transforms.segmentation.DecodeRle
      - _target_: sam3.train.transforms.basic_for_api.RandomResizeAPI
        sizes: ${scratch.resolution}
        max_size:
          _target_: sam3.train.transforms.basic.get_random_resize_max_size
          size: ${scratch.resolution}
        square: true
        consistent_transform: False
      - _target_: sam3.train.transforms.basic_for_api.ToTensorAPI
      - _target_: sam3.train.transforms.basic_for_api.NormalizeAPI
        mean: ${scratch.val_norm_mean}
        std: ${scratch.val_norm_std}

  # Loss — bounding box + segmentation mask
  loss:
    _target_: sam3.train.loss.sam3_loss.Sam3LossWrapper
    matcher: ${scratch.matcher}
    o2m_weight: 2.0
    o2m_matcher:
      _target_: sam3.train.matcher.BinaryOneToManyMatcher
      alpha: 0.3
      threshold: 0.4
      topk: 4
    use_o2m_matcher_on_o2m_aux: false
    loss_fns_find:
      - _target_: sam3.train.loss.loss_fns.Boxes
        weight_dict:
          loss_bbox: 5.0
          loss_giou: 2.0
      - _target_: sam3.train.loss.loss_fns.IABCEMdetr
        weak_loss: False
        weight_dict:
          loss_ce: 10.0
          presence_loss: 10.0
        pos_weight: 10.0
        alpha: 0.25
        gamma: 2
        use_presence: True
        pos_focal: false
        pad_n_queries: 200
        pad_scale_pos: 1.0
      - _target_: sam3.train.loss.loss_fns.Masks
        focal_alpha: 0.25
        focal_gamma: 2.0
        weight_dict:
          loss_mask: 5.0
          loss_dice: 5.0
        compute_aux: false
    loss_fn_semantic_seg: null
    scale_by_find_batch_size: ${scratch.scale_by_find_batch_size}

# ============================================================================
# Helper parameters  (safe defaults — adjust as needed)
# ============================================================================
scratch:
  enable_segmentation: True   # Must be True for binary mask training

  # Model parameters
  d_model: 256
  pos_embed:
    _target_: sam3.model.position_encoding.PositionEmbeddingSine
    num_pos_feats: ${scratch.d_model}
    normalize: true
    scale: null
    temperature: 10000

  # Box/presence processing
  use_presence_eval: True
  original_box_postprocessor:
    _target_: sam3.eval.postprocessors.PostProcessImage
    max_dets_per_img: -1
    use_original_ids: true
    use_original_sizes_box: true
    use_presence: ${scratch.use_presence_eval}

  # Matcher configuration
  matcher:
    _target_: sam3.train.matcher.BinaryHungarianMatcherV2
    focal: true
    cost_class: 2.0
    cost_bbox: 5.0
    cost_giou: 2.0
    alpha: 0.25
    gamma: 2
    stable: False
  scale_by_find_batch_size: True

  # Image processing
  # 1008 is required — lower values are incompatible with the rotary positional encoding.
  resolution: 1008
  consistent_transform: False
  max_ann_per_img: 200

  # Normalisation — ImageNet stats matching SAM3 pretraining distribution.
  train_norm_mean: [0.485, 0.456, 0.406]
  train_norm_std:  [0.229, 0.224, 0.225]
  val_norm_mean:   [0.485, 0.456, 0.406]
  val_norm_std:    [0.229, 0.224, 0.225]

  # DataLoader workers / epochs
  # Set num_*_workers to 0 for debugging; increase to 4+ for full training runs.
  num_train_workers: 4
  num_val_workers: 4
  max_data_epochs: 20
  target_epoch_size: 1500
  hybrid_repeats: 1
  context_length: 2
  gather_pred_via_filesys: false

  # Learning rates
  # Lower lr_scale (e.g. 0.01) if you see NaN losses or gradient explosions.
  lr_scale: 0.02
  lr_transformer:       ${times:8e-4,${scratch.lr_scale}}
  lr_vision_backbone:   ${times:2.5e-4,${scratch.lr_scale}}
  lr_language_backbone: ${times:5e-5,${scratch.lr_scale}}
  lrd_vision_backbone: 0.9
  wd: 0.1
  # Scheduler steps are in optimizer steps (not epochs).
  # With target_epoch_size=1500 and train_batch_size=4, one epoch ≈ 375 steps.
  scheduler_timescale: 375
  scheduler_warmup: 375
  scheduler_cooldown: 2250

  # Batch / collation
  # Reduce train_batch_size (e.g. to 2) if you run out of GPU memory.
  val_batch_size: 2
  collate_fn_val:
    _target_: sam3.train.data.collator.collate_fn_api
    _partial_: true
    repeats: ${scratch.hybrid_repeats}
    dict_key: irrigation canal
    with_seg_masks: ${scratch.enable_segmentation}

  gradient_accumulation_steps: 1
  train_batch_size: 4
  collate_fn:
    _target_: sam3.train.data.collator.collate_fn_api
    _partial_: true
    repeats: ${scratch.hybrid_repeats}
    dict_key: all
    with_seg_masks: ${scratch.enable_segmentation}

# ============================================================================
# Trainer Configuration
# ============================================================================
trainer:

  _target_: sam3.train.trainer.Trainer
  skip_saving_ckpts: false
  empty_gpu_mem_cache_after_eval: True
  skip_first_val: True
  max_epochs: 20
  accelerator: cuda
  seed_value: 42
  val_epoch_freq: 1
  mode: train
  gradient_accumulation_steps: ${scratch.gradient_accumulation_steps}

  distributed:
    backend: null   # set to nccl for multi-GPU / multi-node training

  loss:
    all: ${irrigation_train.loss}
    default:
      _target_: sam3.train.loss.sam3_loss.DummyLoss

  data:
    train:
      _target_: sam3.train.data.torch_dataset.TorchDataset
      dataset:
        _target_: sam3.train.data.sam3_image_dataset.Sam3ImageDataset
        transforms: ${irrigation_train.train_transforms}
        load_segmentation: ${scratch.enable_segmentation}
        max_ann_per_img: 500000
        multiplier: 1
        max_train_queries: 50000
        max_val_queries: 50000
        training: true
        use_caching: False
        # Expects PNG images at <images_root>/train/images/
        img_folder: ${paths.images_root}/train/images/
        # COCO JSON generated by scripts/convert_masks_to_coco.py
        ann_file: ${paths.coco_root}/train/_annotations.coco.json

      shuffle: True
      batch_size: ${scratch.train_batch_size}
      num_workers: ${scratch.num_train_workers}
      pin_memory: True
      drop_last: True
      collate_fn: ${scratch.collate_fn}

    val:
      _target_: sam3.train.data.torch_dataset.TorchDataset
      dataset:
        _target_: sam3.train.data.sam3_image_dataset.Sam3ImageDataset
        load_segmentation: ${scratch.enable_segmentation}
        coco_json_loader:
          _target_: sam3.train.data.coco_json_loaders.COCO_FROM_JSON
          include_negatives: false
          category_chunk_size: 1
          _partial_: true
        img_folder: ${paths.images_root}/val/images/
        ann_file: ${paths.coco_root}/val/_annotations.coco.json
        transforms: ${irrigation_train.val_transforms}
        max_ann_per_img: 100000
        multiplier: 1
        training: false

      shuffle: False
      batch_size: ${scratch.val_batch_size}
      num_workers: ${scratch.num_val_workers}
      pin_memory: True
      drop_last: False
      collate_fn: ${scratch.collate_fn_val}

  model:
    _target_: sam3.model_builder.build_sam3_image_model
    bpe_path: ${paths.bpe_path}
    device: cuda
    eval_mode: false
    enable_segmentation: ${scratch.enable_segmentation}
    checkpoint_path: ${paths.checkpoint_path}
    load_from_HF: true   # set to false once checkpoint_path points to a local file

  meters:
    val:
      irrigation canal:
        detection:
          _target_: sam3.eval.coco_writer.PredictionDumper
          iou_type: "bbox"
          dump_dir: ${launcher.experiment_log_dir}/dumps/irrigation_canal
          merge_predictions: True
          postprocessor: ${scratch.original_box_postprocessor}
          gather_pred_via_filesys: ${scratch.gather_pred_via_filesys}
          maxdets: 100
          pred_file_evaluators:
            - _target_: sam3.eval.coco_eval_offline.CocoEvaluatorOfflineWithPredFileEvaluators
              gt_path: ${paths.coco_root}/val/_annotations.coco.json
              tide: False
              iou_type: "bbox"

  optim:
    amp:
      enabled: True
      amp_dtype: bfloat16   # BF16 is faster and more memory-efficient on Ampere+ GPUs

    optimizer:
      _target_: torch.optim.AdamW
      lr: 1e-6

    gradient_clip:
      _target_: sam3.train.optim.optimizer.GradientClipper
      max_norm: 0.1   # conservative — do not increase without monitoring for NaNs
      norm_type: 2

    param_group_modifiers:
      - _target_: sam3.train.optim.optimizer.layer_decay_param_modifier
        _partial_: True
        layer_decay_value: ${scratch.lrd_vision_backbone}
        apply_to: 'backbone.vision_backbone.trunk'
        overrides:
          - pattern: '*pos_embed*'
            value: 1.0

    options:
      lr:
        - scheduler:
            _target_: sam3.train.optim.schedulers.InverseSquareRootParamScheduler
            base_lr: ${scratch.lr_transformer}
            timescale: ${scratch.scheduler_timescale}
            warmup_steps: ${scratch.scheduler_warmup}
            cooldown_steps: ${scratch.scheduler_cooldown}
        - scheduler:
            _target_: sam3.train.optim.schedulers.InverseSquareRootParamScheduler
            base_lr: ${scratch.lr_vision_backbone}
            timescale: ${scratch.scheduler_timescale}
            warmup_steps: ${scratch.scheduler_warmup}
            cooldown_steps: ${scratch.scheduler_cooldown}
          param_names:
            - 'backbone.vision_backbone.*'
        - scheduler:
            _target_: sam3.train.optim.schedulers.InverseSquareRootParamScheduler
            base_lr: ${scratch.lr_language_backbone}
            timescale: ${scratch.scheduler_timescale}
            warmup_steps: ${scratch.scheduler_warmup}
            cooldown_steps: ${scratch.scheduler_cooldown}
          param_names:
            - 'backbone.language_backbone.*'

      weight_decay:
        - scheduler:
            _target_: fvcore.common.param_scheduler.ConstantParamScheduler
            value: ${scratch.wd}
        - scheduler:
            _target_: fvcore.common.param_scheduler.ConstantParamScheduler
            value: 0.0
          param_names:
            - '*bias*'
          module_cls_names: ['torch.nn.LayerNorm']

  checkpoint:
    save_dir: ${launcher.experiment_log_dir}/checkpoints
    save_freq: 1   # save every epoch

  logging:
    tensorboard_writer:
      _target_: sam3.train.utils.logger.make_tensorboard_logger
      log_dir: ${launcher.experiment_log_dir}/tensorboard
      flush_secs: 120
      should_log: True
    wandb_writer: null
    log_dir: ${launcher.experiment_log_dir}/logs
    log_freq: 10

# ============================================================================
# Launcher and Submitit Configuration
# ============================================================================
launcher:
  num_nodes: 1
  gpus_per_node: 1           # increase for multi-GPU training
  experiment_log_dir: ${paths.experiment_log_dir}
  multiprocessing_context: forkserver

submitit:
  account: null              # SLURM account (leave null for local runs)
  partition: null            # SLURM partition (leave null for local runs)
  qos: null
  timeout_hour: 72
  use_cluster: False         # set to True when submitting to a SLURM cluster
  cpus_per_task: 8
  port_range: [10000, 65000]
  constraint: null
